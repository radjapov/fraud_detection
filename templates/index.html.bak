<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Fraud Detection — Demo</title>

  <!-- Bootstrap CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
      <a class="navbar-brand" href="#">Fraud Detector</a>
      <span class="navbar-text text-muted">Локальная демо-версия</span>
    </div>
  </nav>

  <div class="container">
    <div class="row g-4">
      <div class="col-lg-7">
        <div class="card shadow-sm">
          <div class="card-body">
            <h5 class="card-title">Ввод признаков</h5>
            <p class="text-muted small">Заполни V1..V28 (можно оставить 0) и Amount (рубли) — или нажми "Заполнить пример".</p>

            <form id="predict-form">
              <div class="row" id="features-area">
                <!-- Поля будут заполнены скриптом -->
              </div>

              <div class="mt-3 d-flex gap-2">
                <button id="btn-predict" type="submit" class="btn btn-primary">Оценить</button>
                <button id="btn-sample" type="button" class="btn btn-outline-secondary">Заполнить пример</button>
                <button id="btn-clear" type="button" class="btn btn-outline-danger">Очистить</button>
                <div id="spinner" class="spinner-border text-primary ms-auto d-none" role="status"><span class="visually-hidden">Loading...</span></div>
              </div>
            </form>

            <div class="mt-3">
              <small class="text-muted">Порог (threshold): <span id="threshold-val">{{ threshold }}</span></small>
            </div>
          </div>
        </div>
      </div>

      <div class="col-lg-5">
        <div class="card shadow-sm mb-3">
          <div class="card-body">
            <h5 class="card-title">Результат</h5>
            <div id="result-card" class="p-3 rounded" style="min-height:140px;">
              <h6 id="result-label" class="fw-bold">Здесь появится результат</h6>
              <p id="result-prob" class="mb-1">Вероятность: —</p>
              <p id="result-used" class="mb-0 text-muted">Порог использован: —</p>
            </div>
          </div>
        </div>

        <div class="card shadow-sm">
          <div class="card-body">
            <h6 class="card-subtitle mb-2 text-muted">Инструкция</h6>
            <ul class="small mb-0">
              <li>Ввод можно частичный — отсутствующие поля имитируются медианой.</li>
              <li>Если хочешь протестировать несколько вариаций — используй кнопку "Заполнить пример".</li>
              <li>API: POST /api/predict JSON с ключами признаков.</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>

<script>
  const FEATURES = {{ features|tojson }};
</script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="{{ url_for('static', filename='main.js') }}"></script>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</body>
</html>